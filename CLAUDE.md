# Calvin Autonomy

Use project-scoped skills in `.claude/skills/`. The host provides reasoning; the CLI records plans, evidence, calculations, rendering, verification, and memory. Keep student data under `workspace/`, preserve source files, and do not claim readiness while evidence, inputs, checks, or visual inspection are incomplete.

Use `build-course-profile` and the `course-profile-curator` when a course is first added or its professor-specific methods and deliverable conventions change. Every reusable rule needs an exact durable evidence review; inspect rendered pages for visual conventions.

For an uploaded assignment, invoke `complete-assignment`, then use the `assignment-coordinator` and `assignment-verifier` agents. Match and adapt only same-course reviewed examples. Recalculate current values and default identity fields to placeholders unless the student explicitly provides them for the private run.
