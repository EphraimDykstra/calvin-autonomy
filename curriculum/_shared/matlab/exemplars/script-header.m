% ENGR 315L Control Systems Lab
% Author: [Student Name]
% Date: [MM/DD/YYYY]    version 1
% Title: [Descriptive title of what this script produces]
% File Name: [StudentName]_Lab[N]_v1.m
%
% ILLUSTRATIVE ONLY. Shape of the course template header, not course text.
% Keep "File Name" identical to the name the file is saved under, and bump
% "version" when the file is copied forward to a new experiment.

clc;        % clear the Command Window
close all;  % close every open figure, so the script's figure is the only one
clear all;  % empty the workspace, so no stale variable leaks into this run
            % (skip this line when a Simulink model reads workspace variables)

% Code starts here:
